pipeline {
    agent none 
    stages {
        stage('Build') { 
            agent {
                docker {
                    image 'python:3.9' 
                }
            }
            steps {
                sh 'python3 -m pip install -r requirements.txt' 
            }
        }
    }
}